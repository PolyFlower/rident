import React from "react";

type Props = {};

const SectionText = (props: Props) => {
	return (
		<div className="section-one-text">
			<span className="one">Find the best deal!</span>
			<span className="two">Choose from wide range of options.</span>
		</div>
	);
};

export default SectionText;
